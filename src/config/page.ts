class PageConfig {
  // TODO: Change homepage after dashboard is finished
  static home = "/dashboard";
  static operation = "/operation-follow";
  static login = "/login";
}

export default PageConfig;
